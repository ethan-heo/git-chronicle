{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run GitChronicle",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "outFiles": ["${workspaceFolder}/dist/extension/**/*.js"],
      "preLaunchTask": "npm: build"
    }
  ]
}
